#!/bin/bash
#firewall settings is: auto, on , off only!!
firewall="auto"

#lan bypass settings is: on or off only!!
lan_bypass="on"

# port settings is: 443, 80, 53, 123, 1194, 54783
port="443"

# protocol settings is: UDP, STEALTH, TCP
protocol="UDP"